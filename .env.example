# Copy to `.env` for Vite runtime env vars (they must be prefixed with VITE_).
# Example:
# VITE_GAME_BUILD=2026.04.23

# Any `VITE_*` variable here is inlined into the client bundle at build time.
# DO NOT put secrets in this file — everything is public.
